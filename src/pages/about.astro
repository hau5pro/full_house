---
import BaseLayout from "../layouts/base-layout.astro";
---

<BaseLayout title="About | Full House">
  <div class="container-narrow about-page">
    <section class="about-section">
      <h2>Just Recipes. No Life Stories.</h2>
      <p>
        Welcome to Full House, a personal recipe collection that gets straight
        to the point. No lengthy anecdotes about someone's grandmother's
        cousin's neighbor. No endless scroll through life stories before you can
        find out how long to boil an egg.
      </p>
      <p>
        This is a no-BS recipe book where you get what you came for: the
        ingredients, the instructions, and maybe a helpful note or two. That's
        it.
      </p>
    </section>

    <div class="about-cta">
      <a href="/" class="cta-button">Browse Recipes</a>
    </div>
  </div>
</BaseLayout>

<style>
  .about-page {
    padding-top: var(--space-xl);
    padding-bottom: var(--space-3xl);
  }

  .about-section {
    margin-bottom: var(--space-2xl);
    padding-bottom: var(--space-xl);
    border-bottom: 1px solid var(--border);
  }

  .about-section:last-of-type {
    border-bottom: none;
  }

  .about-section h2 {
    font-size: var(--text-2xl);
    font-weight: 600;
    color: var(--accent);
    margin-bottom: var(--space-lg);
    display: flex;
    align-items: center;
    gap: var(--space-sm);
  }

  .about-section h2::before {
    content: "";
    width: 4px;
    height: 24px;
    background: var(--accent);
    border-radius: 2px;
  }

  .about-section p {
    font-size: var(--text-lg);
    line-height: 1.8;
    color: var(--text);
    margin-bottom: var(--space-md);
  }

  .about-section p:last-child {
    margin-bottom: 0;
  }

  .about-cta {
    margin-top: var(--space-3xl);
    text-align: center;
  }

  .cta-button {
    display: inline-block;
    padding: var(--space-md) var(--space-xl);
    background: var(--accent);
    color: var(--bg);
    font-size: var(--text-lg);
    font-weight: 600;
    border-radius: var(--border-radius-md);
    text-decoration: none;
    transition: all var(--transition-base);
    box-shadow: var(--shadow-md);
  }

  .cta-button:hover {
    transform: translateY(-2px);
    box-shadow: var(--shadow-lg), var(--shadow-glow);
    opacity: 1;
  }

  @media (max-width: 768px) {
    .about-section h2 {
      font-size: var(--text-xl);
    }

    .about-section p {
      font-size: var(--text-base);
    }
  }
</style>
